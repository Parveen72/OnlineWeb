import { ConfirmEqualValidatorDirectiveDirective } from './confirm-equal-validator-directive.directive';

describe('ConfirmEqualValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEqualValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
